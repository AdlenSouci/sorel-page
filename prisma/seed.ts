/**
 * Les données catalogue sont dans MySQL (sorel_local).
 * Ce seed ne fait rien — évite d'écraser la base existante.
 */
console.log("Seed ignoré : utilisez les tables categories / catalogue dans sorel_local.");
