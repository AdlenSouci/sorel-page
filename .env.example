# MySQL — base catalogue (tables categories + catalogue)
DATABASE_URL="mysql://root@127.0.0.1:3306/sorel_local"

# API Express (npm run dev:api ou dev:full)
API_PORT=3001

# Front Vite : laisser vide en local (proxy /api → :3001)
VITE_API_URL=
