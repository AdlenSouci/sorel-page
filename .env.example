# Local : laisser vide (npm run dev:full → MySQL local via /api)
VITE_SOREL_ORDER_URL=

# Vercel : URL du site Laravel (même base que les tables categories / catalogue)
# VITE_SOREL_ORDER_URL=https://sorel-order.fr

DATABASE_URL=
API_PORT=3001
